div(class="tileItem visualIEFloatFix")
    a(title="${title}", href="${url}", class="tileImage")
        img(alt="${title}",title="${title}",src="${thumbUrl}")
    div(class="tileContent")
        h2(class="tileHeadline", id="data-centre-overview")
            a(title="${title}", class="state-published", href="${url}") ${title}
        span(class="date") ${date}
        p(class="eea-tileUrl") ${url}
    p(class="tileBody")
        span(class="description") ${description}
    p(class="tileFooter")
        a(class="moreLink", href="${url}") Read more
    div(style="padding-bottom:1.0em;", class="visualClear") 
